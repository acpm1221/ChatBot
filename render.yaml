services:
  - type: web
    name: docchat-server
    runtime: node
    rootDir: server
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 5000
